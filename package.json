{
  "name": "kanboard-cli",
  "version": "1.0.0",
  "description": "Professional CLI for Kanboard",
  "main": "src/bin/index.ts",
  "bin": {
    "kanboard-cli": "./src/bin/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "build": "bun build ./src/bin/index.ts --outfile ./dist/kanboard --target bun",
    "build:deb": "./build-deb.sh"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chalk": "^4.1.2",
    "cli-table3": "^0.6.3",
    "commander": "^11.1.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
